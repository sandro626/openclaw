# Server Config Skill Copy

`server-config/skills/qrcoin` 已退出当前活跃树，不再作为技能真源。

当前归属：

- 活跃源码：`overlay/skills/qrcoin/`
- 历史副本：`.artifacts/ops/archive/server-config-skills/qrcoin-<timestamp>/`

不要把新的技能修改再写回这里。
