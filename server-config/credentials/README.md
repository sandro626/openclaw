# Credentials Archive

`server-config/credentials/` 已退出活跃树。

真实运行态文件应放在：

- `~/.openclaw/credentials/*.json`

本仓库只保留不带敏感值的结构样例：

- `runtime-templates/state/credentials/allow-from.example.json`
- `runtime-templates/state/credentials/pairing.example.json`

历史原件已归档到：

- `.artifacts/ops/archive/server-config-runtime-20260327T095906Z/server-config/credentials/`
