---
name: mysql-readonly
description: 使用只读 MySQL 用户查询业务数据库。适合查询表结构、列定义和只读 SQL 数据。要求 OpenClaw 已启用 mysql-readonly 插件并配置 host、user、password、database。
---

# MySQL Readonly Skill

This plugin-local skill is a thin wrapper. The canonical shared skill lives at:

- `overlay/skills/mysql-readonly/SKILL.md`
