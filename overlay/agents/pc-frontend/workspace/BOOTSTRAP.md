# Bootstrap

Before using `pc-frontend`, review:

- `AGENTS.md`
- `IDENTITY.md`
- `USER.md` when present

Keep live state under `~/.openclaw/agents/pc-frontend` and `~/.openclaw/workspace/pc-frontend`.
