# Bootstrap

Before using `pc-pctester`, review:

- `AGENTS.md`
- `IDENTITY.md`
- `USER.md` when present

Keep live state under `~/.openclaw/agents/pc-pctester` and `~/.openclaw/workspace/pc-pctester`.
