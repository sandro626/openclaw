# Agent Workspace

This workspace belongs to agent `{{agentId}}`.

Use this directory for agent-local instructions and safe static context.

Do not store sessions, secrets, or generated runtime state here through source control.
