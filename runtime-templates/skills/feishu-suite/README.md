# Feishu Skill Runtime Templates

此目录用于存放飞书相关技能的运行态模板。

建议内容：

- 技能启用清单模板
- 技能环境变量样例
- 技能配置模板
- `feishu-contacts` 的 `FEISHU_APP_ID`、`FEISHU_APP_SECRET`、账户文件路径、缓存目录、共享目录、日志路径和文档 URL
- Lark / Feishu bridge 的 `FEISHU_APP_ID`、secret path、webhook 端口、`OPENCLAW_HOME` 和 `OPENCLAW_CONFIG_PATH` 模板
